<%= form_for(@additive) do |f| %>
  <% if @additive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@additive.errors.count, "error") %> prohibited this additive from being saved:</h2>

      <ul>
      <% @additive.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
